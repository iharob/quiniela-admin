node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json

# TypeScript build info
*.tsbuildinfo

# Local CI (.ci/) — runner lives in the standalone `cihere` repo
.ci/.state/
.ci/secrets.local.yaml
.ci-artifacts/
