# Base URL of the quiniela REST API, including the /api/v1 prefix.
# Local dev example:
VITE_API_BASE_URL=http://localhost:8000/api/v1
# Production example (same host as the site):
# VITE_API_BASE_URL=https://iaales.lat/api/v1
